<view class="page">
  <view class="head" bindtap="show">
  </view>
  <view class="img-box">
    <view class="content" wx:for="{{array}}">
      <image class="pic" src="{{item.pic}}"></image>
       <view>{{item.name}}</view> 
    </view>
  </view>
</view>